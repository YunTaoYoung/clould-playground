# Cloud Playground - 需求文档

## 项目概述

**Cloud Playground** 是一个技术学习平台，旨在通过自动化环境搭建和数据准备，降低开发者学习新技术的门槛。

### 核心价值
- 一键激活学习环境（自动部署K8s资源）
- 自动初始化样本数据
- 提供交互式练习界面
- 记录学习历史和进度

### 用户场景
- 单用户本地部署工具
- 开源项目，用户自行安装使用
- 前置条件：用户已安装Docker Desktop和配置好kubectl

---

## 功能需求

### 1. 主题管理系统

#### 1.1 主题定义
每个主题包含：
- 元数据（ID、名称、版本、描述、类别）
- K8s资源定义（Deployment、Service、ConfigMap等）
- 连接配置（主机、端口、凭证）
- 初始化脚本（可选）
- 题目列表

#### 1.2 主题生命周期
```
未激活 → 激活中 → 运行中 → 停用中 → 已停用
```

**激活流程：**
1. 验证K8s连接可用
2. 应用K8s资源（Deployment + Service）
3. 等待Pod就绪（health check）
4. 获取Service访问端点（NodePort）
5. 执行初始化脚本（如有）
6. 保存主题状态到本地数据库

**停用流程：**
1. 删除K8s资源（Deployment、Service）
2. 可选：保留PVC（数据持久化）
3. 更新主题状态

#### 1.3 支持的主题（MVP）
1. **PostgreSQL** - 关系型数据库学习
2. **MySQL** - 关系型数据库学习
3. **Redis** - 缓存与数据结构
4. **RabbitMQ** - 消息队列
5. **Java** - 编程语言（本地JVM执行）

### 2. 题目系统

#### 2.1 题目类型

| 类型 | 适用主题 | 验证方式 |
|------|----------|----------|
| `sql-query` | PostgreSQL/MySQL | 自动验证（结果集对比） |
| `redis-command` | Redis | 自动验证（返回值对比） |
| `messaging` | RabbitMQ | 手动验证（观察） |
| `code-challenge` | Java | 自动验证（JUnit测试） |

#### 2.2 题目结构
```yaml
id: ex001
title: "连表查询练习"
difficulty: beginner | intermediate | advanced
type: sql-query
description: "查询每个部门的员工数量和平均薪资"
files:
  - setup.sql      # 建表+插入样本数据
  - solution.sql   # 参考答案（可选）
validation:
  type: auto       # auto | manual
  expected_result_file: expected.json  # 预期结果
  performance_threshold: 100ms  # 性能要求（可选）
```

#### 2.3 题目执行流程
1. 用户选择题目
2. 系统执行setup脚本（初始化数据）
3. 用户在编辑器中编写代码/SQL
4. 点击"运行"按钮
5. 后端执行代码并返回结果
6. 显示执行结果、性能指标
7. 自动验证（如适用）
8. 保存执行记录到历史

### 3. 用户界面

#### 3.1 主题市场页
- 展示所有可用主题（卡片式）
- 显示主题状态（未激活/运行中/已停用）
- 激活/停用按钮
- 查看主题详情

#### 3.2 主题详情页
- 主题介绍
- 章节/模块列表
- 题目列表（带难度标签）
- 学习进度概览
- 资源状态监控

#### 3.3 练习页面
**布局：**
- 左侧：题目描述 + 提示
- 中间：代码/SQL编辑器（Monaco Editor）
- 右侧：结果展示区
  - 表格形式展示查询结果
  - 执行时间、查询计划
  - 验证结果（✅/❌）

**功能按钮：**
- 运行（Ctrl+Enter快捷键）
- 重置数据
- 查看参考答案
- 保存到历史

#### 3.4 历史记录页
- 按主题/题目筛选
- 时间线展示
- 点击可查看历史代码和结果
- 导出功能（可选）

### 4. 数据持久化

#### 4.1 本地数据库（H2）
存储内容：
- 主题激活状态
- 题目完成记录
- 练习历史（代码、结果、时间戳）
- 用户配置（K8s context等）

#### 4.2 K8s资源数据
- 使用PVC持久化数据库数据（可选）
- 主题停用时可选择保留/删除数据

---

## 技术架构

### 系统架构图
```
┌──────────────────────────────────────────────┐
│              Browser (React)                  │
│  ┌────────────┬──────────────┬─────────────┐ │
│  │ 主题市场   │ 练习界面     │ 历史记录    │ │
│  └────────────┴──────────────┴─────────────┘ │
└────────────────────┬─────────────────────────┘
                     │ HTTP/WebSocket
┌────────────────────▼─────────────────────────┐
│         Spring Boot Backend                   │
│  ┌──────────────────────────────────────┐    │
│  │ Theme Manager │ K8s Client          │    │
│  │ Exercise Engine │ History Service    │    │
│  └──────────────────────────────────────┘    │
│  │ Local H2 Database (metadata/history) │    │
└────────────┬────────────────────┬────────────┘
             │                    │
             │ Kubernetes API     │ JDBC
             ▼                    ▼
┌─────────────────────┐  ┌──────────────────┐
│  Kubernetes Cluster │  │ PostgreSQL Pod   │
│  (Docker Desktop)   │  │ MySQL Pod        │
│                     │  │ Redis Pod        │
│  ┌───────────────┐  │  │ RabbitMQ Pod     │
│  │ Pods+Services │  │  └──────────────────┘
│  └───────────────┘  │
└─────────────────────┘
```

### 技术栈

**后端：**
- Java 17+
- Spring Boot 3.x
- Spring Data JPA
- Kubernetes Java Client (`io.kubernetes:client-java`)
- HikariCP（动态数据库连接池）
- H2 Database（嵌入式）
- WebSocket（实时执行反馈）

**前端：**
- React 18
- TypeScript
- Ant Design（UI组件库）
- Monaco Editor（代码编辑器）
- React Query（数据获取）
- Zustand（状态管理）
- Axios（HTTP客户端）

**基础设施：**
- Docker Desktop（含Kubernetes）
- kubectl（用户需自行配置）

---

## 数据模型

### 本地数据库（H2）Schema

```sql
-- 主题状态表
CREATE TABLE theme_status (
    id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    status VARCHAR(20) NOT NULL, -- NOT_ACTIVATED, ACTIVATING, RUNNING, STOPPING, STOPPED
    namespace VARCHAR(100),
    service_endpoint VARCHAR(255),
    activated_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- 题目完成记录
CREATE TABLE exercise_completion (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    theme_id VARCHAR(50) NOT NULL,
    exercise_id VARCHAR(50) NOT NULL,
    completed BOOLEAN DEFAULT FALSE,
    score INT,
    completed_at TIMESTAMP,
    UNIQUE(theme_id, exercise_id)
);

-- 练习历史
CREATE TABLE exercise_history (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    theme_id VARCHAR(50) NOT NULL,
    exercise_id VARCHAR(50) NOT NULL,
    code TEXT NOT NULL,
    result TEXT,
    execution_time_ms BIGINT,
    success BOOLEAN,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 用户配置
CREATE TABLE user_config (
    key VARCHAR(100) PRIMARY KEY,
    value TEXT,
    updated_at TIMESTAMP
);
```

### 主题配置格式（YAML）

```yaml
# themes/postgresql/theme.yaml
id: postgresql
name: PostgreSQL 数据库
version: 1.0.0
description: 学习PostgreSQL核心特性和高级查询
category: database
icon: 🐘

# Kubernetes资源定义
kubernetes:
  namespace: cloud-playground  # 或动态生成
  deployment:
    name: postgresql
    image: postgres:15-alpine
    replicas: 1
    resources:
      requests:
        memory: "256Mi"
        cpu: "250m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    env:
      - name: POSTGRES_PASSWORD
        value: "learningpwd"
      - name: POSTGRES_DB
        value: "playground"
    ports:
      - containerPort: 5432
        name: postgres
    volumeMounts:
      - name: data
        mountPath: /var/lib/postgresql/data

  service:
    name: postgresql-svc
    type: NodePort
    ports:
      - port: 5432
        targetPort: 5432

  persistentVolumeClaim:  # 可选
    name: postgresql-pvc
    storageClassName: hostpath
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi

# 连接配置模板
connection:
  driver: org.postgresql.Driver
  url: "jdbc:postgresql://{host}:{port}/playground"
  username: postgres
  password: learningpwd

# 主题初始化脚本
init_scripts:
  - path: scripts/init_database.sql
    type: sql

# 题目定义
exercises:
  - id: pg-basic-001
    title: "基础SELECT查询"
    difficulty: beginner
    category: "基础查询"
    description: |
      学习PostgreSQL的基本SELECT语法。
      查询employees表中所有员工的姓名和部门。
    type: sql-query
    setup_script: exercises/pg-basic-001/setup.sql
    solution_script: exercises/pg-basic-001/solution.sql
    validation:
      type: auto
      expected_result: exercises/pg-basic-001/expected.json

  - id: pg-join-001
    title: "内连接查询"
    difficulty: intermediate
    category: "连表查询"
    description: |
      使用INNER JOIN连接员工表和部门表。
      查询每个部门的员工数量和平均薪资。
    type: sql-query
    setup_script: exercises/pg-join-001/setup.sql
    solution_script: exercises/pg-join-001/solution.sql
    validation:
      type: auto
      expected_result: exercises/pg-join-001/expected.json
      performance_threshold_ms: 100
```

---

## MVP实施计划

### Phase 1: 基础框架（Week 1-2）

**后端：**
- [x] Spring Boot项目初始化
- [x] H2数据库配置和Schema创建
- [x] Kubernetes Java Client集成
- [x] 主题配置加载器（YAML解析）
- [x] ThemeService（激活/停用逻辑）
- [x] 基础REST API（/api/themes, /api/exercises）

**前端：**
- [x] React + TypeScript项目初始化
- [x] Ant Design集成
- [x] 路由配置（主题市场、练习页、历史记录）
- [x] 主题市场页面UI
- [x] Axios配置和API Service层

### Phase 2: 题目系统（Week 3）

**后端：**
- [x] ExerciseService（题目加载、执行管理）
- [x] 动态数据库连接管理
- [x] SQL执行引擎
- [x] 结果验证器
- [x] WebSocket实时反馈

**前端：**
- [x] Monaco Editor集成
- [x] 练习页面UI
- [x] 结果展示组件
- [x] WebSocket客户端

### Phase 3: 主题实现（Week 4-5）

**数据库主题：**
- [x] PostgreSQL主题配置
  - 5个题目（基础查询、JOIN、子查询、索引优化、窗口函数）
- [x] MySQL主题配置
  - 5个题目（类似PostgreSQL）

**其他主题：**
- [x] Redis主题
  - 3-5个题目（String/Hash/List操作、过期策略）
- [x] RabbitMQ主题
  - 3个题目（点对点、发布订阅、路由模式）
- [x] Java主题（简化版）
  - 3个题目（算法、集合、多线程）

### Phase 4: 用户体验（Week 6）

- [x] 练习历史记录功能
- [x] 主题详情页完善
- [x] 错误处理和友好提示
- [x] Loading状态和进度提示
- [x] 基础测试覆盖

### Phase 5: 文档和部署（Week 7）

- [x] README.md（安装指南、使用说明）
- [x] 开发文档
- [x] Docker Compose一键启动（可选）
- [x] 演示视频/GIF

---

## 非功能需求

### 性能
- 主题激活时间：< 30秒（取决于镜像拉取）
- SQL查询响应：< 2秒（一般查询）
- 前端首次加载：< 3秒

### 安全性
- K8s资源隔离（使用独立namespace）
- SQL注入防护（PreparedStatement）
- Java代码执行限制（MVP暂不做沙箱，信任用户）

### 可维护性
- 代码规范（ESLint + Prettier）
- 日志记录（SLF4J + Logback）
- 错误追踪

### 可扩展性
- 插件系统预留接口（后续支持用户自定义主题）
- 主题配置标准化（YAML schema）

---

## 风险与假设

### 假设
1. 用户已安装Docker Desktop并启用Kubernetes
2. 用户已配置好kubectl并切换到正确的context
3. 用户本地端口30000-32767可用（NodePort范围）
4. 用户了解基本的K8s概念

### 风险
1. **K8s环境差异**：不同系统（Windows/Mac/Linux）的Docker Desktop行为可能不同
   - 缓解：提供详细的环境检查工具
2. **资源限制**：用户机器性能不足
   - 缓解：提供最低配置要求说明
3. **端口冲突**：NodePort端口被占用
   - 缓解：支持自定义端口范围配置
4. **镜像拉取失败**：网络问题或镜像源限制
   - 缓解：提供国内镜像源配置说明

---

## 后续扩展计划（Post-MVP）

1. **插件市场**
   - 用户可上传自定义主题
   - 在线浏览和安装插件

2. **协作功能**
   - 分享练习方案
   - 社区题库

3. **高级验证**
   - 查询计划分析
   - 性能评分系统

4. **多环境支持**
   - 支持远程K8s集群
   - 云环境部署

5. **AI助手**
   - 题目提示
   - 代码审查建议

---

## 附录

### 项目结构

```
cloud-playground/
├── backend/                    # Spring Boot后端
│   ├── src/main/java/
│   │   └── com/cloudplayground/
│   │       ├── config/         # 配置类
│   │       ├── controller/     # REST Controllers
│   │       ├── service/        # 业务逻辑
│   │       ├── repository/     # JPA Repositories
│   │       ├── model/          # 实体类和DTOs
│   │       ├── kubernetes/     # K8s客户端封装
│   │       └── executor/       # 代码执行引擎
│   ├── src/main/resources/
│   │   ├── application.yml     # 应用配置
│   │   ├── schema.sql          # H2数据库Schema
│   │   └── themes/             # 主题配置文件
│   │       ├── postgresql/
│   │       ├── mysql/
│   │       ├── redis/
│   │       ├── rabbitmq/
│   │       └── java/
│   └── pom.xml
│
├── frontend/                   # React前端
│   ├── src/
│   │   ├── components/         # React组件
│   │   ├── pages/              # 页面组件
│   │   ├── services/           # API Services
│   │   ├── stores/             # Zustand状态管理
│   │   ├── types/              # TypeScript类型定义
│   │   └── utils/              # 工具函数
│   ├── package.json
│   └── tsconfig.json
│
├── docs/                       # 文档
│   ├── REQUIREMENTS.md         # 本文档
│   ├── API.md                  # API文档
│   └── DEVELOPMENT.md          # 开发指南
│
├── CLAUDE.md                   # Claude Code指引
└── README.md                   # 项目说明
```

---

**文档版本：** 1.0
**最后更新：** 2025-11-09
**状态：** 已确认
